## Packages
framer-motion | Complex animations for correct/incorrect feedback
canvas-confetti | Celebration effect for correct answers
lucide-react | Icons for UI
clsx | Class name utility (if not already present)
tailwind-merge | Class name utility (if not already present)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
